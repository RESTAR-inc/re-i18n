import type { I18nConfig } from "../types";

export function jsonExport(config: I18nConfig) {
  // const entries = getRawData(config);
  // const exportData: I18nRawDataOLD = {
  //   entries,
  //   meta: {
  //     datetime: new Date().toUTCString(),
  //   },
  // };
  // const outDirPath = path.resolve(config.json.outDir);
  // console.log(`Writing JSON files to ${outDirPath}`);
  // if (!fs.existsSync(outDirPath)) {
  //   fs.mkdirSync(outDirPath, { recursive: true });
  // }
  // fs.writeFileSync(
  //   path.resolve(config.json.outDir, "i18n.json"),
  //   JSON.stringify(exportData, null, 2),
  //   {
  //     encoding: "utf8",
  //   }
  // );
}
